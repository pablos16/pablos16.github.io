import Game from './constants.js'

const CT =
{
     xDialogImage: Game.gameWidth / 2,
     yDialogImage: Game.gameHeight / 1.25 + 50,

     dialogSpacing: 10,
     xDialogTextPos: Game.gameWidth / 5.25,
     yDialogTextPos: Game.gameHeight - 72 * 2.5 - 25 + 50,
     xDialogNamePos: Game.gameWidth / 5.25 + 35,
     yDialogNamePos: Game.gameHeight - 72 * 3.25 - 25 + 50,
     ySubDialogSpacing: 30,
     xSubDialogSpacing: 50,
     subDialogInSpacing: 40,
     xDialogSelection: -20,
     yDialogSelection: 15,
     dialogFont: 'font',
     dialogSize: 30,
     subDialogSize: 25,
     dialogAlign: 0,
     dialogSpeed: 10,
     characterOffset: 50,

     textLimit: 50,
}

export default CT;