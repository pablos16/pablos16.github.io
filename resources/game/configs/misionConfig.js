import Game from './constants.js'

const CT =
{
     misionListX: Game.gameWidth / 2,
     misionListY: Game.gameHeight,
     misionOffsetToggle: 300,
     hideOffset: 50,

     misionTextSize: 27.5,
     subMisionTextSize: 20,
     xMisionText: Game.gameWidth / 8,
     yMisionText: 0,
     yMissionOffset: 60,
     ySubMisionTextOffset: 25
}

export default CT;