import Game from './constants.js'

const CT =
{
     misionListX: Game.gameWidth * 0.8,
     misionListY: Game.gameHeight * 1.2,
     misionOffsetToggle: 300,
     hideOffset: 50,

     misionTextSize: 27.5,
     font: 'misionText',
     subFont: 'subMision',
     subMisionTextSize: 20,
     xMisionText: -212,
     yMisionText: -144,
     yMissionOffset: 60,
     ySubMisionTextOffset: 25
}

export default CT;